{
  "providers": [
    {
      "id": "agentrouter",
      "displayName": "AgentRouter",
      "description": "Integrasi model AI dari agentrouter.org",
      "config": {
        "model": {
          "type": "string",
          "description": "Nama model di AgentRouter (mis. ar-gpt-1)"
        },
        "apiKeySecretName": {
          "type": "string",
          "description": "Nama env/secret yang menyimpan API key (default: AGENTROUTER_API_KEY)",
          "default": "AGENTROUTER_API_KEY"
        }
      }
    }
  ]
}
